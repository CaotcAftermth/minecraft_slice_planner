import React from 'react'
type Props={ open:boolean; onClose:()=>void; children?:React.ReactNode }
export default function SettingsModal({open,onClose,children}:Props){ if(!open) return null; return (<div aria-modal="true" role="dialog" onClick={onClose} style={{ position:'fixed', inset:0, background:'rgba(0,0,0,.5)', display:'grid', placeItems:'center', zIndex:50 }}><div onClick={(e)=>e.stopPropagation()} style={{ background:'#1f2937', color:'#e5e7eb', borderRadius:12, padding:16, width:520, maxWidth:'90vw' }}><div style={{ display:'flex', alignItems:'center', justifyContent:'space-between', marginBottom:12 }}><h3 style={{ margin:0, fontSize:18 }}>Settings</h3><button onClick={onClose} aria-label="Close" style={{ fontSize:18, lineHeight:1, cursor:'pointer' }}>✕</button></div>{children ?? <p>Keybinds and preferences will appear here.</p>}</div></div>) }
